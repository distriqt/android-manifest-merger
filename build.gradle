plugins {
    id 'java'
}

group 'com.distriqt.android'
version '31.9.0'

repositories {
    mavenCentral()
    google()
}

dependencies {
    testImplementation group: 'junit', name: 'junit', version: '4.12'

    // https://mvnrepository.com/artifact/com.android.tools.build/manifest-merger
    compile group: 'com.android.tools.build', name: 'manifest-merger', version: '31.9.0'
    compile group: 'com.android.tools', name: 'common', version: '31.9.0'

}

jar {
    manifest {
        attributes "Main-Class": "com.distriqt.android.ManifestMerger"
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    } {
        exclude 'META-INF/*.RSA', 'META-INF/*.SF', 'META-INF/*.DSA'
    }
}